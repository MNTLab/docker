node('docker') {

 stage('Preparation (Checking out)') {
       git branch: 'pavel_yurchuk2', url: 'https://github.com/PaulYurchuk/docker.git'
         }

 stage ('Building docker gradle image'){
	  docker.build ('gradle', '-f docker-2/gradle.Dockerfile .')
            }
 dir('docker-2'){
    stage ('Building code'){
        docker.image('gradle:latest').inside {
             sh 'gradle build'
    	    }
	       }

	stage ('Deployment'){
       	        sh 'docker-compose up -d'
	            }

	stage ('Check'){

	         sh 'sleep 8 && curl -IL http://127.0.0.1:8085'
             sh 'docker-compose stop jar && docker-compose rm -f jar'
             }

	        }

 }
